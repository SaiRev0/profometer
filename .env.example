# ===========================================
# ProfOMeter Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# ===========================================
# Authentication (Google OAuth)
# ===========================================
# Get these from Google Cloud Console
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ===========================================
# Database (PostgreSQL via Supabase)
# ===========================================
# Connection pooling URL (for most queries)
DATABASE_URL=postgresql://user:password@host:5432/database?pgbouncer=true

# Direct connection URL (for migrations)
DIRECT_URL=postgresql://user:password@host:5432/database

# ===========================================
# NextAuth Configuration
# ===========================================
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here

# Base URL of your application
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Uncomment for production
# NEXTAUTH_URL=https://your-domain.com

# ===========================================
# Anonymous User Configuration
# ===========================================
# User ID for anonymous reviews (from database)
ANONYMOUS_USER_ID=your-anonymous-user-id

# ===========================================
# Review Security Keys
# ===========================================
# RSA key pair for anonymous review encryption (Base64 encoded)
# Generate RSA keys and encode to Base64
ANON_REVIEW_PUBLIC_KEY=your-base64-encoded-public-key
ANON_REVIEW_PRIVATE_KEY=your-base64-encoded-private-key

# Hash secret for anonymous reviews
# Generate with: openssl rand -hex 32
ANON_REVIEW_HASH_SECRET=your-hash-secret-here

# ===========================================
# Review Duplicate Prevention
# ===========================================
# Secret for generating review commitments (duplicate review prevention)
# IMPORTANT: Generate a secure random string (64+ characters)
# Once set, DO NOT change - this will break duplicate detection
# Generate with: openssl rand -hex 32
COMMITMENT_SECRET=

# Review cycle identifier (format: YYYY_MM_X where X is A/B for first/second half)
REVIEW_CYCLE_ID=2025_01_A

# Optional: Override cycle duration for testing (in months, default: 2)
# REVIEW_CYCLE_MONTHS=2
